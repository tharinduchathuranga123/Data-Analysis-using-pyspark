Student Depression Analysis using PySpark

This repository contains a **PySpark-based analysis** of student depression datasets. The project includes data preprocessing, analysis, risk scoring, and recommendations using PySpark, SQL, and visualization libraries.

---

## **Installation**

Install the required Python packages:

```bash
!pip install -q pyspark
!pip install -U -q PyDrive
!pip install -q matplotlib seaborn scikit-learn pandas
